export interface PokemonType {
  id: string;
  name: string;
  url: string;
  image: string;
  type: string;
  weight: string;
  experience: string;
}

export interface PokemonListType {
  name: string;
}